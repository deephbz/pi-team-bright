# v0.17.0-rc.14 release checklist

Status: selected source prepared; isolated product acceptance passed. Mark an
item complete only with an exact receipt reference.

## Source candidate

- [ ] Record the selected source base and candidate commit.
- [x] Set package and lock versions to `0.17.0-rc.14` in the prepared tree.
- [x] Update package image and README install references in the prepared tree.
- [ ] Update the parity checklist from the verified candidate.
- [ ] Update current release context without rewriting dated journals.
- [ ] Check JSON, Markdown links, version consistency, and `git diff --check`.
- [ ] Commit the exact candidate.

## Reversible gates

- [ ] Run `npm ci --workspaces=false`.
- [ ] Run `TERM_PROGRAM=iTerm.app npm run test:full`.
- [ ] Run `npm run test:lanes`.
- [ ] Run `npm run verify:package`.
- [ ] Run `npm run qa:agent-surface`.
- [ ] Run `npm run qa:tool-results`.
- [ ] Run `git-privacy-scan --ref HEAD history`.
- [ ] Push the candidate commit after every gate passes.
- [ ] Run the hosted publish workflow with `dry_run=true`, `tag=next`, and a
  unique nonce. Confirm its commit equals the candidate commit.

## Irreversible publication

- [x] Owner authorized the complete rc.14 release operation in this Session.
- [ ] Create and push annotated tag `v0.17.0-rc.14` at the gated commit.
- [ ] Run the hosted publish workflow with `dry_run=false`, `tag=next`, and a
  new nonce.
- [ ] Verify npm version, `next` tag, integrity, downloaded tarball, and
  provenance.
- [ ] Create the GitHub prerelease from the annotated tag.
- [ ] Append the rc.14 release receipt with all immutable identifiers.

Do not use this checklist as proof. The receipt is the publication evidence.
