# v0.17.5 stable patch release checklist

Status: local patch candidate. No `v0.17.5` tag, npm version, or GitHub Release
exists.

Scope: publish owner-approved, decision-oriented operating guidance for Team
coordination. The packaged skill teaches long-lived Team identity, minimum
sufficient Worker topology, causal-context reuse, deliberate fresh perspective,
and the one lead's project-lead and coordinator responsibilities. It also guides
the lead to call `team_sync({view:"updates"})` after interim messages while
assigned nonterminal Tasks remain, including waiting and blocked Tasks.

## Release identity

- Package: `@hypercarrier/pi-team-bright`
- Version: `0.17.5`
- Annotated tag: `v0.17.5`
- Expected npm dist-tag: `latest`
- GitHub title: `Pi Team Bright v0.17.5`
- GitHub release type: stable
- Source base: `origin/main` at `d5ed077f1ddc1af2b76aa104e3938a6481c2e20d`
- Release notes: [`v0.17.5-release-notes.md`](v0.17.5-release-notes.md)

## Compatibility and boundaries

Version `0.17.5` changes packaged agent operating guidance only. It changes no
Team storage schema, Task graph, model-tool, Membership, runtime, terminal, or
Worker protocol contract. The sync ordering is guidance; it does not claim
runtime enforcement or permanent subscription behavior.

- Team identity follows a project or durable coordination boundary.
- Worker identity follows coherent semantic scope or deliberate perspective
  isolation.
- Task identity follows one bounded outcome.
- The project lead preserves user intent, constraints, approach, decomposition,
  outcomes, and trade-offs; delegation does not transfer accountability.
- The coordinator assigns Tasks, supplies context, resolves blockers, supervises
  outcomes, and escalates decisions to the user.
- A new request, terminal Task graph, empty ready front, or idle interval does
  not prove Team completion.
- A new Worker must unlock parallelism, establish reusable scope, or isolate
  perspective.
- Dependent implementation, execution, diagnosis, and repair normally preserve
  one Worker's causal context.
- Independent review and alternative experiments may use fresh Workers.
- Task graph assignment and goal or dependency changes stay separate from Alerts
  and context updates.
- While assigned nonterminal Tasks remain, interim user replies, progress notes,
  and blocker escalations end with `team_sync({view:"updates"})`, unless the
  owner explicitly pauses or stops the work. Avoid identical empty sync loops by
  naming the blocker and next actor.
- `team_sync` is a bounded observation and wait call. `caught_up` and
  `indeterminate` can return while Tasks remain nonterminal.
- Do not copy private machine paths or live Team payloads into tracked content.
- Use the nonce-v1 GitHub Actions OIDC workflow for `latest` publication.

## Candidate gates

- [ ] Recheck packaged skill structure against the exact final tree.
- [ ] Rerun focused public-surface and package verification against the exact
  final tree.
- [ ] Regenerate the package dry-run receipt from the exact final tree.
- [ ] The exact final tree passes one aggregate local release lane.
- [ ] One reviewed single-commit pull request merges to `main` after CI passes.

## Release workflow

- [ ] Confirm merged source equals exact `origin/main` and CI is green.
- [ ] Confirm npm `0.17.5`, Git tag `v0.17.5`, and GitHub Release are absent.
- [ ] Pack once from a clean detached checkout and record immutable integrity.
- [ ] Install the exact tarball into an isolated project.
- [ ] Pass a nonce-correlated hosted dry-run with publication skipped.
- [ ] Create and push annotated tag `v0.17.5`.
- [ ] Pass nonce-correlated OIDC publication with `tag: latest`.
- [ ] Verify registry integrity, provenance, signatures, and `latest` routing.
- [ ] Create the stable GitHub Release from the reviewed notes.
