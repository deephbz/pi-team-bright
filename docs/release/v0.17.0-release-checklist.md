# v0.17.0 stable release checklist

Status: stable source preparation. Publication evidence is pending.

Scope: promote the verified DAG-native line to `main`, publish
`@hypercarrier/pi-team-bright@0.17.0` through GitHub Actions trusted
publishing, set npm `latest`, and create the stable GitHub Release.

## Release identity

- Package: `@hypercarrier/pi-team-bright`
- Version: `0.17.0`
- Annotated tag: `v0.17.0`
- npm dist-tag: `latest`
- GitHub title: `Pi Team Bright v0.17.0`
- GitHub release type: stable
- Source base: `origin/main` at
  `9f4649e228c612dbe0fed2124676830ff438644f`
- Release notes: [`v0.17.0-release-notes.md`](v0.17.0-release-notes.md)

## Pre-main evidence

Executable release source `f06c450263d2b43b9ccb41b36107da21821feb6d`,
tree `e1e359df541c0e0977716d6784bffb8607075f75`, passed the complete local
release lane. The aggregate passed 142 files and 1,007 tests; 8 files and 16
tests were intentionally skipped. Test-lane closure reported 143 files: 120
fast and 23 exhaustive. Package and generated-output verification passed. The
sequential package dry run reported 197 files, 330,250 packed bytes, and
1,430,511 unpacked bytes.

The independent exact-source Team E2E ran before the final one-line lifecycle
stage correction. That correction changed documentation only. Two stable
Terra-medium Workers exercised all nine leader tools and all three Worker
tools. Six Tasks reached `goal_achieved`; one bounded criterion failure produced
`criterion@1 -> build@2 -> criterion@2`; a success join bound `criterion@2`
and `notes@1`; graph replay and changed-operation refusal passed; both Workers
stopped; shutdown retained no unfinished Tasks. Durable Team event and exact
Worker Session evidence independently proved Worker `task_read`, `task_update`,
and `alert_send`.

Version `0.17.0`, tag `v0.17.0`, and the matching GitHub Release were absent at
source selection. npm did not contain `@hypercarrier/pi-team-bright@0.17.0`.

The exact stable-preparation full-history privacy scan rejected 100 inherited
findings across 24 commits. Every finding commit is an ancestor of publication
baseline `325fe3b69fe6127808b1fdb0dcf3e3d707635a1a`; this does not certify
inherited history. The candidate range after that baseline passed.

## Boundaries

- Preserve all existing tags and published package bytes.
- Never replace, move, or force-update a published ref.
- Push the selected release source to `main` with a compare-and-swap lease.
- Use GitHub Actions OIDC for npm publication. Do not use or expose an npm
  token.
- Dispatch the dry run before the tag or real publication.
- Publish the real workflow from the immutable annotated tag with `latest`.
- Verify registry integrity and provenance before creating the GitHub Release.
- Record downstream HyperCarrier composition separately after publication.

## Gates

- [x] Rebase the complete DAG-native line onto current `origin/main`.
- [x] Set package and lock versions to `0.17.0`.
- [x] Refine the public README and stable release notes.
- [x] Update maintained current context for stable release selection.
- [x] Confirm `0.17.0`, `v0.17.0`, and the GitHub Release are absent.
- [x] Run the exact-source integration E2E through an independent Pi agent.
- [x] Run `test:full`, test-lane closure, and package/generated verification.
- [x] Inspect one sequential package dry run.
- [ ] Pack once from a clean detached checkout and record SHA-512.
- [ ] Install and probe that exact tarball in an isolated project.
- [x] Run and classify full-history privacy at the exact source.
- [x] Pass privacy range from the configured publication baseline.
- [ ] Push the exact release source to `main`.
- [ ] Confirm Node 22 and Node 24 CI pass for the exact main commit.
- [ ] Dispatch and verify the GitHub publish workflow with `dry_run: true`.
- [ ] Create and push annotated tag `v0.17.0`.
- [ ] Dispatch the tagged workflow with `dry_run: false` and `latest`.
- [ ] Verify npm version, integrity, provenance, and `latest`.
- [ ] Create the stable GitHub Release from the reviewed notes.
- [ ] Commit the final publication receipt to `main`.
