# v0.17.2 stable patch release checklist

Status: local patch candidate. No ref, registry, or GitHub Release action has
occurred.

Scope: replace the unreleased Herdr accepted-start probe and fallback with one
official interactive-ready command. Keep the readiness timeout distinct from
exact Session-binding observation. Preserve the remaining Worker startup
optimizations and defer concurrent `ensure_worker` design.

## Release identity

- Package: `@hypercarrier/pi-team-bright`
- Version: `0.17.2`
- Annotated tag: `v0.17.2`
- Expected npm dist-tag: `latest`
- GitHub title: `Pi Team Bright v0.17.2`
- GitHub release type: stable
- Source base: `origin/main` at `6ffd44599891878887544862446faf53e186ef5c`
- Release notes: [`v0.17.2-release-notes.md`](v0.17.2-release-notes.md)

## Compatibility

Version `0.17.2` is backward compatible with `0.17.1` and `0.17.0` Team state.
It does not change Team storage, Task graph, model-tool, or Worker protocol
contracts. Operators do not recreate a Team for this patch.

## Boundaries

- Preserve `origin/main`, `v0.17.1`, and their package bytes.
- Do not implement concurrent `ensure_worker` execution in this patch.
- Do not retain an unpublished Herdr option, version probe, or fallback branch.
- Do not copy private machine paths into tracked content.
- Do not push, tag, publish, change a dist-tag, or create a GitHub Release during
  candidate preparation.
- Use GitHub Actions OIDC for stable `latest` publication.

## Candidate gates

- [x] Use only the official Herdr `agent start` command with `--timeout 6000`.
- [x] Remove accepted-start capability, fallback, and binding-budget plumbing.
- [x] Keep exact ready-response, target, and canonical Pi argv validation.
- [x] Pass focused adapter, observation, compensation, and type checks: 51 tests
  and typecheck passed.
- [x] Pass a real Herdr 0.7.5 Worker launch, exact Session binding, Task
  transition, stop, and cleanup canary from the candidate source. The sanitized
  machine receipt is
  [`2026-08-19-v0.17.2-real-herdr-canary.json`](../journal/artifacts/2026-08-19-v0.17.2-real-herdr-canary.json).
- [x] Pass the final aggregate, lane closure, package verification, and package
  dry-run from the stable source. Lane closure reports 143 files: 120 fast and
  23 exhaustive. The package dry-run reports 197 files, 329,909 packed bytes,
  and 1,428,473 unpacked bytes.
- [x] Run the full-history privacy scan at the candidate commit. It reported
  only inherited pre-baseline commit-metadata findings. The protected candidate
  range passed with no candidate-location finding.
- [ ] Merge one single-commit pull request to `main` after CI passes.

## Release workflow

- [ ] Confirm the merged commit is exact `origin/main` and CI is green.
- [ ] Confirm npm version `0.17.2` and Git tag `v0.17.2` are absent.
- [ ] Pack once from a clean detached checkout and record the tarball SHA-512.
- [ ] Install that exact tarball into an isolated project and run the Pi canary.
- [ ] Pass a hosted dry-run with publication skipped.
- [ ] Create and push annotated tag `v0.17.2`.
- [ ] Pass tagged OIDC publication with `tag: latest`.
- [ ] Verify registry bytes, integrity, provenance, signatures, and `latest`.
- [ ] Create the stable GitHub Release from the reviewed notes.
