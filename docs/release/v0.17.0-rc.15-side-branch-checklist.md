# v0.17.0-rc.15 DAG-native side-branch checklist

Status: side-branch candidate. This is not an npm publication receipt.

Scope: prepare `release/v0.17.0-rc.15-dag-native` for review without updating
`main`, creating a Git tag or GitHub Release, or changing npm or its dist-tags.

## Source identity

- Base publication record: `325fe3b69fe6127808b1fdb0dcf3e3d707635a1a`.
- Candidate version: `0.17.0-rc.15`.
- The reviewed DAG range will contain five semantic commits: orchestration;
  graph control and pane; acceptance and release gates; terminal-environment
  fixes; and the RC candidate hardening record.
- The pre-rewrite history is retained locally under
  `backup/dag-native-before-release-squash-fd62c24`.

## Reversible gates

- [x] Confirm `0.17.0-rc.15` is absent from npm before preparation.
- [x] Make graph status colors use terminal-owned semantic ANSI roles.
- [x] Pass focused Task graph tests and typecheck for the color change.
- [x] Prove the rewritten tip tree equals the pre-rewrite tip tree.
- [x] Run the final aggregate lane on the exact versioned tree. It passed after
  stale legacy public-contract characterizations moved out of the graph-native
  acceptance surface.
- [x] Run test-lane, package, generated-output, and agent-surface gates. The
  lane contract reports 138 tests: 115 fast and 23 exhaustive.
- [x] Inspect `npm pack --dry-run --json`: version `0.17.0-rc.15` and 188
  files. Treat byte sizes as derived output because this receipt changes them.
- [x] Run the configured privacy range scan from the RC14 baseline.
- [x] Push only `release/v0.17.0-rc.15-dag-native`.
- [x] Confirm the remote branch targets the exact reviewed source.

## Explicit exclusions

Do not push `main`. Do not create or push `v0.17.0-rc.15`. Do not dispatch the
npm publish workflow. Do not mutate npm, an npm dist-tag, or a GitHub Release.
Those operations require a later release decision and the mainline release
contract in [`../maintainers/releasing.md`](../maintainers/releasing.md).
