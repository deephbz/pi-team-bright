# Pi Team Bright v0.17.3 release receipt

Date: 2026-08-19
Stage: sharing
Architecture impact: none

Stable patch source is
`fada68bf65e85bfb105ac03ec1a8d97bec03cff1`, tree
`cbe7a112485ec01385eb75d548f5c3b73999cd06`. Annotated tag `v0.17.3` has tag
object `4fd910643ae3b8b8eb5dc22f11b24c6b6dd9e0a8` and targets that source.

## Product result

The Herdr adapter names the exact pane handle returned by `pane split` with the
stable Worker name before Agent startup. It uses the official `pane rename`
command. Label failure emits a warning and `carrier_label_not_applied` trace,
then Worker coordination continues. This changes presentation only. It adds no
public tool, Team storage, Task graph, Membership, or Worker protocol contract.

## Source and hosted gates

PR [#12](https://github.com/deephbz/pi-team-bright/pull/12) carried one reviewed
source commit. Exact-source CI
[32241033307](https://github.com/deephbz/pi-team-bright/actions/runs/32241033307)
passed on Node 22 and Node 24. A clean detached source passed `test:full`, all
143 lane files, package verification, an isolated package install, and a real
Herdr Team with two named Worker panes.

Hosted dry-run
[32241058837](https://github.com/deephbz/pi-team-bright/actions/runs/32241058837)
passed from the exact source and skipped publication. Hosted OIDC publish run
[32241628625](https://github.com/deephbz/pi-team-bright/actions/runs/32241628625)
checked out `refs/tags/v0.17.3`, reran the aggregate, lane, and package gates,
and published with `npm publish --access public --provenance --tag latest`.
No local npm token publication occurred.

The hosting service first added a merge carrier with the same source tree. The
managed privacy gate rejected tagging that carrier. The rejected cutoff was
audited outside the repository, and no later commit existed. A compare-and-swap
update replaced it with the reviewed single source commit. The candidate range
and managed pre-push gate passed. Full history still contains inherited
pre-baseline findings, which this receipt does not certify.

## Registry artifact and provenance

npm reports `@hypercarrier/pi-team-bright@0.17.3`. `latest` selects `0.17.3`;
`next` remains `0.17.0-rc.14`.

- Files: `197`
- Packed bytes: `330242`
- Unpacked bytes: `1429658`
- npm shasum: `1cdbbf14559beb0d54f0bfe3c7c46c9336bc6c09`
- npm integrity:
  `sha512-+vGDjwx8msn3/JHwM8OkrPnnP2mDhW2vhw9OuEpeaBNArHICgsPYfRYgxPKI43+pcN3eOifSuGKk6ZMiyZQWlQ==`
- SHA-256: `04c93d534f6fcc781f3e61398caf9fb6912fa60ddd396dbc23961b28e4c0a8e8`
- SHA-512:
  `faf1838f0c7c9ac9f7fc91f033c3a4acf9e73f6983856daf870f4eb84a5e681340ac720282c3d87d1620c4f288e37fa970ddde3a27d2b862a4e99322c9941695`

The downloaded registry tarball matched the reviewed local artifact byte for
byte. npm exposes a registry signature and SLSA v1 provenance. An isolated
install verified all 256 dependency registry signatures and 54 attestations.

Stable GitHub Release:
https://github.com/deephbz/pi-team-bright/releases/tag/v0.17.3

GitHub Releases remain release-history authority. Downstream HyperCarrier
compatibility records and gitlinks remain separate composition evidence.
