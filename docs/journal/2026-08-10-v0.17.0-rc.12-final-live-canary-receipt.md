# v0.17.0-rc.12 final live canary receipt

Date: 2026-08-10
Result: passed
Publication state: local only; no push, tag, npm publication, or registry change

## Tested source

The canary loaded the working-tree extension from local commit
`e8b42b0cd80fb30ca4f7ba1347cda99c95d0a380`. That commit contains semantic-
hardening base `ec7642a2d54c44b0c941048c74f14695c87f5c9d` and reports package version
`0.17.0-rc.12`.

Pi 0.83.0 started the fresh coordinator through `_codex_with_proxy` with
`openai-codex/gpt-5.6-terra:medium`. The coordinator confirmed that
`HTTPS_PROXY`, `ALL_PROXY`, and `NO_PROXY` were present without printing their
values. An isolated Pi configuration set the same model for Workers and stopped
global extension discovery from loading another Pi Team Bright copy.

The canary did not use `agentLoopReady` as evidence. It did not use a manual
exact-Session fallback.

## DAG and execution evidence

Team `rc12-final-audit-canary-20260810-final2` committed operation
`rc12-final-canary-op-2` as one keyed three-Task chain:

- `plan` was Task `rc12-final-audit-canary-20260810-final2-syi`, assigned to
  `maker`;
- `impl` was Task `rc12-final-audit-canary-20260810-final2-oiu`, assigned to
  `maker`, and needed `plan`;
- `review` was Task `rc12-final-audit-canary-20260810-final2-3tb`, assigned to
  `reviewer`, and needed `impl`.

Independent Beads reads after execution showed the two prerequisite relations,
the expected assignees, and all three Tasks closed. The append-only Team event
journal showed this Worker-authored sequence:

1. cursor 8: `maker` claimed `plan`;
2. cursor 9: `maker` recorded result evidence and closed `plan`;
3. cursor 10: `maker` claimed `impl`;
4. cursor 11: `maker` recorded result evidence and closed `impl`;
5. cursor 12: `reviewer` claimed `review`;
6. cursor 13: `reviewer` recorded result evidence and closed `review`.

Thus `impl` was not claimed before the `plan` result, and `review` was not
claimed before the `impl` result. The leader made no `task_update` call. It
used `team_sync` for normal progress, so ordinary DAG handoff was mechanical.

## Cleanup and proof limits

Both Workers stopped. Team shutdown succeeded with no unfinished Task IDs. The
durable Team record marks the leader, `maker`, and `reviewer` Memberships
inactive. The Team event cursor reached 15 after both Worker stop events. The
created coordinator pane and isolated Pi configuration were removed; durable
Team and Task records remain as raw local evidence.

This canary proves one live three-stage chain, two stable Workers, repeated work
on one Worker, cross-Worker handoff, Worker-authored events, and clean shutdown.
It does not prove arbitrary graph size, throughput, or failure recovery. The
machine-operable receipt is
[`artifacts/2026-08-10-v0.17.0-rc.12-final-live-canary.json`](artifacts/2026-08-10-v0.17.0-rc.12-final-live-canary.json).
