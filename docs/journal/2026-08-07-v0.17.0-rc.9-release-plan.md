# v0.17.0-rc.9 source release plan

Date: 2026-08-07
Stage: sharing and release hardening
Source base: reviewed commit `79cea8c893a01b978f38f6e99368537697e9e3cb`
Final source: blocker fixes `cb6e8bbc61573b295b518e90ee225e6f649f77ff` and resumed-nudge binding `8e56afc` integrated on the release branch
Target: `release/v0.17.0-rc.9` in `/private/tmp/ptb-rc9-release`

## Scope

Prepare release-source artifacts only. Set the package and lockfile version to
`0.17.0-rc.9`, update the README install and image references, and align the
model-tool implementation version. Update current and intent documentation for
normal `caught_up` and `indeterminate` outcomes, the Pi `>=0.83` boundary,
global wait and nudge settings, the enabled/1200-second defaults, the narrow
Worker surface, and failed-event completeness. Preserve historical receipts.

Carry the coordination research handoff and the exact implementation
continuation journal from the leader worktree, then append final decisions and
verification results without rewriting prior evidence. Add the rc.9 parity
checklist. The final source uses bounded sequential Beads hydration batches of
16 IDs, typed Task-authority failures, Pi 0.84 footer subscription validation,
and exact resumed-leader binding before nudge-debt reads. Keep architecture impact **none**. Do not merge,
tag, push, publish, or run the broad suite.

## Focused verification

- Validate JSON package metadata and lockfile structure.
- Search nonhistorical source and docs for stale rc.8 references.
- Run documentation/model-tool generation checks.
- Run package-focused and coordination-liveness checks only.
- Record exact commit and changed files after the final source tree is stable.

Publication remains a separate owner action after independent verification.

## Preparation result

The source candidate uses exact `79cea8c` plus accepted blocker commits
`cb6e8bbc61573b295b518e90ee225e6f649f77ff` and `8e56afc`, and has no stale
nonhistorical rc.8 reference. Bounded Beads hydration uses batches of at most 16
IDs. Task-authority failures return typed unavailable results without staging;
Pi 0.84 footer validation and resumed-leader nudge binding have focused proofs.

Final focused checks passed: JSON validation, model-tool documentation
generation, TypeScript 5.9 typecheck, 101 tests across nine files, packed
observation probing, and generated-dist comparison. No broad suite or
publication was run.
