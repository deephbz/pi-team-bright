# Pi Team Bright v0.17.0-rc.12 live canary receipt

Date: 2026-08-10
Stage: hardening
Result: passed on the pre-subsystem-split source
Evidence status: historical; this receipt does not verify the later audit-branch rebase
Architecture impact: changed inside Pi Team Bright; HyperCarrier's opaque
Pi Team Bright view remains unchanged

## Result

A fresh Pi 0.83.0 coordinator started through `_codex_with_proxy` with
`openai-codex/gpt-5.6-terra:medium`. Its exact tool child confirmed that
`HTTPS_PROXY`, `ALL_PROXY`, and `NO_PROXY` were present. An isolated Pi
configuration loaded this working-tree extension once and selected
`openai-codex/gpt-5.6-terra` for both Workers.

Team `rc12-dag-canary-tmux-8` bound fresh `maker` and `reviewer` Sessions. One
`task_create` operation committed a three-Task chain. `maker` handled `plan` and
`impl`; `reviewer` handled `review`. The leader made no Task update and used no
separate relation or dispatch call.

The event record shows this order:

1. Both Worker Sessions bound.
2. The atomic operation created all three assigned Tasks.
3. `maker` claimed and closed `plan` with result evidence.
4. Task authority presented `impl`; `maker` claimed and closed it.
5. Task authority presented `review`; `reviewer` claimed and closed it with a
   PASS result.
6. Both Workers stopped, then Team shutdown reported no unfinished Tasks.

The Worker-authored events at cursors 8 through 13 are the end-to-end launch and
mechanical-delivery signal. The redacted machine receipt is
[`artifacts/2026-08-10-v0.17.0-rc.12-live-canary.json`](artifacts/2026-08-10-v0.17.0-rc.12-live-canary.json).
Raw Team, delivery, Session, provider, proxy, and terminal records remain outside
Git.

## Diagnostic note

During cleanup, tmux reported that each Worker pane was already absent. Durable
Worker-stopped events and Team shutdown both completed. This warning does not
change Task or Team authority evidence, but it remains visible in the machine
receipt.

## Proof boundary

This canary proves one live atomic DAG and mechanical successor path across two
real Worker Sessions. It does not prove throughput, contention behavior, or
parallel execution inside one Worker Session. Aggregate, package, generated
output, public-content, persistence, and privacy gates remain separate. No tag,
npm publication, distribution-tag change, registry claim, or GitHub release is
authorized by this receipt.
