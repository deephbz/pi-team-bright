# Pi Team Bright rc.12 audit-rebase live canary receipt

Date: 2026-08-10
Stage: hardening
Result: passed on the recorded pre-final-rebase working tree
Architecture impact: changed inside Pi Team Bright; HyperCarrier keeps Pi Team
Bright opaque in its canonical architecture view

## Result

A fresh Pi 0.83.0 coordinator started through `_codex_with_proxy` with
`openai-codex/gpt-5.6-terra:medium`. Its tool child confirmed that
`HTTPS_PROXY`, `ALL_PROXY`, and `NO_PROXY` were present without recording their
values. Isolated settings selected `openai-codex/gpt-5.6-terra` for both Workers.
No manual Session fallback was used.

One atomic operation created `plan -> impl -> review` across `maker` and
`reviewer`. Worker-authored result events at cursors 9, 11, and 13 recorded
`0.17.0-rc.12`. The leader made no Task update. Authority reads after shutdown
show all three Tasks closed, terminal, and free of active blockers. Both Workers
stopped, Team shutdown succeeded, and no active Membership remained.

The redacted machine receipt is
[`artifacts/2026-08-10-v0.17.0-rc.12-audit-rebase-live-canary.json`](artifacts/2026-08-10-v0.17.0-rc.12-audit-rebase-live-canary.json).
Raw Team, Session, delivery, and terminal records remain in their native stores.

## Diagnostics and repair

The first attempt exposed Herdr's 32-character agent-name limit. Team-qualified
Worker carrier names could exceed that limit. The adapter now keeps an already
valid short name and otherwise uses a deterministic, lowercase, 32-character
name with a hash suffix. Focused Herdr and tmux tests pass.

A second attempt omitted the isolated Worker default-model setting. One Worker
selected unavailable Anthropic auth, so that run was aborted and cleaned up
without a success claim. This was a canary-fixture defect. The successful run
set the Worker model explicitly in isolated Pi settings.

## Source and proof boundary

The run used committed head `774f8a3f0e08bc9245e156f1f5552adff57ee64f`
on audit base `a74350e8ca43eae00f2434fbbabd9fc7ce9c60f3`, plus the exact
working-tree file digests in the machine receipt. The audit branch advanced
after this run, so this receipt is historical evidence and does not verify a
later final rebase.

The canary proves one live atomic DAG and mechanical successor path across two
Worker Sessions. It does not prove throughput, contention behavior, or parallel
execution inside one Worker Session. It authorizes no branch push, tag, npm
publication, registry change, provenance claim, or GitHub release.
