# v0.17.0-rc.10 release plan

Date: 2026-08-08
Stage: sharing
Architecture impact: none

## Purpose

Publish the package-version compatibility correction from `2ded422`. The release removes the npm release string as a Team compatibility gate. New Teams omit package provenance, while historical `implementationVersion` values remain accepted.

## Source preparation

Prepare `0.17.0-rc.10` package, lockfile, install example, image URL, and release checklist metadata. Remove the obsolete package-version test constant so a future release bump cannot recreate the false storage concept.

## Acceptance

The exact final source must pass privacy, full test, lane, generated, package, exact-tarball installation, and fresh Herdr integration E2E gates. The E2E must prove a historical Team config remains operable through leader and Worker tools. Hosted dry-run, tagged OIDC publication under npm `next`, registry byte/integrity/provenance checks, and a GitHub prerelease must complete before the publication receipt.

Do not replace rc.9 bytes or retarget its tag. Publish rc.10 as a strictly higher version.
