# v0.17.0-rc.12 finalized-base side-branch canary

Date: 2026-08-11
Result: passed
Publication state: local only; no push, tag, npm publication, or registry change

## Source and branch boundary

The canary loaded the local working-tree extension from commit
`82e3b417c1ff2ee7d05bacc0fe28a37c4bb3d92b` on
`rc/v0.17.0-rc.12`. Its merge base with
`audit/semantic-hardening-behavior-inventory` was the finalized hardening commit
`dfe1552784abf92d4426f48912594b12e415a755`. The DAG branch had two commits
beyond that base. The semantic-hardening branch remained unchanged. The package
reported `0.17.0-rc.12`.

Pi 0.83.0 started the coordinator through `_codex_with_proxy` with
`openai-codex/gpt-5.6-terra:medium`. It confirmed `HTTPS_PROXY`, `ALL_PROXY`, and
`NO_PROXY` were present without printing values. An isolated Pi configuration
selected the same Worker model and prevented another Pi Team Bright copy from
loading. The canary did not use `agentLoopReady` or a manual exact-Session
fallback as evidence.

## DAG execution

Team `rc12-dfe-final-canary-20260811b` committed operation
`rc12-dfe-final-canary-op-2` as one atomic graph:

- `plan` was Task `rc12-dfe-final-canary-20260811b-ozt`, assigned to `maker`;
- `impl` was Task `rc12-dfe-final-canary-20260811b-68d`, assigned to `maker`,
  and needed `plan`;
- `review` was Task `rc12-dfe-final-canary-20260811b-512`, assigned to
  `reviewer`, and needed `impl`.

Independent Beads reads showed both dependency edges, the expected assignees,
and all Tasks closed. The append-only Team event journal showed:

1. cursor 8: `maker` claimed `plan`;
2. cursor 9: `maker` recorded result evidence and closed `plan`;
3. cursor 10: `maker` claimed `impl`;
4. cursor 11: `maker` recorded result evidence and closed `impl`;
5. cursor 12: `reviewer` claimed `review`;
6. cursor 13: `reviewer` recorded result evidence and closed `review`.

Thus each successor claim followed its prerequisite result. The leader made no
`task_update` call and used `team_sync` for normal progress. Both Workers
provided read-only command evidence.

## Cleanup and preceding invalid attempt

Both Workers stopped. Team shutdown succeeded with no unfinished Task IDs. The
durable record marks all three Memberships inactive. The event cursor reached
15 after both Worker stop events. The coordinator pane and isolated Pi
configuration were removed. Durable Team and Task records remain as raw local
evidence.

A preceding attempt used Team `rc12-dfe-final-canary-20260811`. The coordinator
omitted all three Task assignees despite the requested mapping, so no Task could
dispatch. It made no product success claim. Shutdown stopped both Workers,
deactivated all Memberships, and retained three open Tasks as historical Task
authority. The successful retry used explicit assignees and a new Team and
operation identity.

This canary proves one live three-stage chain, repeated work on one Worker,
cross-Worker handoff, Worker-authored events, and clean successful-Team
shutdown. It does not prove arbitrary graph size or throughput. The
machine-operable receipt is
[`artifacts/2026-08-11-v0.17.0-rc.12-dfe-side-branch-live-canary.json`](artifacts/2026-08-11-v0.17.0-rc.12-dfe-side-branch-live-canary.json).
