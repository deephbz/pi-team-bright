# Pi Team Bright v0.17.0-rc.12 source verification receipt

Date: 2026-08-10
Stage: sharing candidate
Verified source: `e2bb51697b3b62c9334edd7b5a38429b32875283`
Package metadata: `@hypercarrier/pi-team-bright@0.17.0-rc.12`
Publication state: historical pre-rebase candidate; untagged and unpublished
Evidence status: this receipt does not verify the later subsystem-split rebase
Architecture impact: changed inside Pi Team Bright; HyperCarrier's opaque
Pi Team Bright view remains unchanged

## Scope and source identity

This receipt records verification of exact source commit
`e2bb51697b3b62c9334edd7b5a38429b32875283`. The branch rebases the DAG-native
contract on semantic-hardening rc.11 base
`a3c51d78c3e95549768a91a36535bee0b5c94755`. The source commit preserves the
injected Task reconciliation and mutation-publication boundaries, then adds
Task-owned graph orchestration and ready-front selection.

This document is commit-relative evidence added after source verification. It
is not part of the verified source identity. No rc.12 tag, npm publication,
npm distribution-tag change, registry provenance claim, or GitHub release
occurred.

## Aggregate and focused repair

The first reserved Node 22 aggregate found six stale outside-in
characterization cases that still invoked the removed batch-create contract.
Those cases also exposed a real recovery gap: ready-front enqueue failure did
not persist the existing delivery-recovery record. The repair moved those tests
to atomic graph creation and made ready delivery persist recovery evidence
before it returns an enqueue failure.

Focused verification then passed:

- two outside-in characterization files and seven tests;
- publication and ready-dispatch files with nine tests;
- two real-Beads files and five tests in 49.99 seconds;
- six model-surface files and 41 tests; and
- five orchestration and terminal-adapter files with 33 tests.

The repaired final aggregate ran under Node 22.22.1:

```sh
fnm exec --using 22.22.1 -- env TERM_PROGRAM=iTerm.app npm run test:full
```

It passed 95 files and 717 tests in 273.09 seconds. Test-lane closure passed
with 95 files: 72 fast and 23 exhaustive. Agent-surface QA, tool-result QA,
model-tool probing, package verification, generated-distribution comparison,
TypeScript, JSON parsing, changed-Markdown links, package metadata, and diff
checks passed. The generated model-tool review contains 287,869 bytes; its
contract digest is `cefa9ce429dfff8aaedf14782905ff20f4212ba7fc5efdbf96a350c7a2eace4f`.

## Live multi-Session canary

A fresh Pi 0.83.0 coordinator and two exact Worker Sessions passed one atomic
`plan -> impl -> review` chain. `maker` handled the first two Tasks and
`reviewer` handled the third. Worker-authored claim, result, and close events
prove mechanical successor delivery without a leader Task update. Both Workers
stopped and Team shutdown reported no unfinished Tasks.

The exact runtime source bundle digest is
`ec2d3ff07a7e302ea39f67c34143c6a94e13a14bde34f719101d9d965f5685be`.
The redacted machine evidence is in the [live canary
receipt](artifacts/2026-08-10-v0.17.0-rc.12-live-canary.json).

## Exact one-pack procedure

The verified source tree ran `npm pack --json` once for the final artifact.
The exact tarball installed in a clean temporary project under Node 22.22.1.
It contained 99 files. npm reported 239,992 packed bytes and 1,028,485 unpacked
bytes.

- npm shasum: `a6986f4f9d92bbdd613539378bafd2211ef316f6`
- npm integrity:
  `sha512-vpi0AIi+0xkLmPFxysz0X9/lPJBZTent8LrPLPNSJ5nwi+aBs9ckS/mrO/T2k6vCZqKBf55ucgmvh4H52+nRHA==`
- tarball SHA-256:
  `c5c5984aaa5698cf310f445c02ca99709caa0102eb9067a4c0c6476348580f17`
- tarball SHA-512:
  `be98b40088bed3190b98f171caccf45fdfe53c90594de9edf0bacf2cf3522799f08be681b3d7244bf9ab3bf4f693abc266a2817f9e6e7209af8781f9dbe9d11c`
- path-sorted manifest SHA-256:
  `72770b99c7e7db61649649f0a51f5270108323037f82f656b362474eec51851d`

The manifest digest uses path-sorted JSON `{path,size,mode}` entries with one
final LF.

## Installed-artifact canaries

The installed exact tarball exposed the observation package through CommonJS
and TypeScript. Its owned Beads binary reported 1.1.0. A bounded no-model Pi
0.83.0 RPC canary loaded the installed main extension, returned state, and
emitted no extension error.

These checks prove package wiring, observation exports, owned Beads resolution,
and extension load. They do not replace the live source-tree Team journey or
prove high-contention behavior.

## Privacy and publication boundary

The configured privacy range through the verified source commit passed. The
full-history scan reports only grandfathered pre-baseline commit-metadata
locations: its status and output are byte-identical to the configured-baseline
scan. The range is clean, but this receipt does not certify grandfathered
history for publication.

The push authorization recorded at verification time was later withdrawn before
this candidate was rebased. This receipt does not authorize a branch push, tag,
npm publication, registry change, provenance claim, or GitHub release.
