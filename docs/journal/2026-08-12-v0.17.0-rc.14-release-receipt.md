# v0.17.0-rc.14 release receipt

Date: 2026-08-12
Stage: sharing

Release source is `8bb517bd32d8687e97b96a531db15833fd64420a`. Annotated tag
`v0.17.0-rc.14` has tag object `923ed5408786e433ef771b528164c006b3f588b6`
and targets that exact source.

## Product acceptance

The isolated exact-source macOS E2E used a Sol-high leader and eight
Terra-medium Workers. All nine Tasks closed with Worker-authored evidence.
Logical Worker `e2e-1` stopped, recovered once, and closed a second Task. All
current Workers stopped, and Team shutdown returned no unfinished Task IDs.

The external monitor matched 27 non-overlapping `team_sync` calls. Product-call
duration was 16 to 5,308 ms, no call exceeded 15 seconds, and at most one live
descendant Beads list process appeared. Seventy-four one-second samples had no
observed FSEvents storm. `fs_usage` required root and supplied no acceptance
evidence. The detailed evidence is in the [exact-source E2E
receipt](2026-08-12-rc14-isolated-exact-source-e2e-receipt.md).

## Source and package gates

The clean committed source ran `npm ci --workspaces=false`. Its one reserved
local aggregate passed 127 isolated Vitest result blocks and 895 assertions;
56 per-case assertions were skipped by the causal isolation harness. The log
SHA-256 is `d9ae3b9599033ebad14ae663336181107ba99c1fa5b6948ae4a3922be705a7a2`.

Lane closure reported 120 files: 99 fast and 21 exhaustive. Package and
generated-output verification, agent-surface QA, tool-result QA, version, JSON,
317 local Markdown links, public observation and generated-dist comparison,
persistence-coordinate comparison, diff checks, and the release-range privacy
scan passed. Independent Terra-medium verification repeated the changed-boundary
and static checks and accepted the candidate.

A clean detached checkout packed once. The exact tarball installed in a clean
project and passed CommonJS and TypeScript observation probes, owned Beads
1.1.0, and a bounded no-model Pi RPC extension-load canary.

- Files: `165`
- Packed bytes: `244906`
- Unpacked bytes: `1059736`
- npm shasum: `5946e00d839f7669932a6c381b57d5e3b97bcbae`
- npm integrity: `sha512-76L4G06LWDmqlP1C5QVOLOFyyK4aXScwbXEren7Ua66c5t+zeJnFGL08wEZPm1g7BWnI/XUtAtIhSrzneZATHg==`
- SHA-256: `736f926a9e86e5c9c214f4f0a3ea7ef9bd6b7c2113a8801cf60c9668618c673c`
- SHA-512: `efa2f81b4e8b5839aa94fd42e5054e2ce172c8ae1a5d27306d712b7a7ed46bae9ce6dfb37899c518bd3cc0464f9b583b0569c8fd752d02d2214abce77990131e`

## Hosted verification and publication

Main CI run `31589330598` passed the exact source on Node 22 and Node 24. Hosted
dry-run `31589737265` passed its full test, lane, and package gates at the same
source and skipped publication.

Hosted OIDC publish run `31592109336` passed from annotated tag
`v0.17.0-rc.14`. It ran the full aggregate, lane, and package gates before
`npm publish --access public --provenance --tag next`.

Registry verification found version `0.17.0-rc.14`, and npm `next` resolves to
that version. The downloaded registry tarball matches the one-pack receipt in
byte count, shasum, integrity, SHA-256, and SHA-512. Registry attestations
contain SLSA provenance v1.

GitHub prerelease:
https://github.com/deephbz/pi-team-bright/releases/tag/v0.17.0-rc.14

The configured release-range privacy scan passed. Full-history findings remain
only grandfathered pre-baseline metadata locations, so this receipt does not
certify older history.

## Post-publication integration

Publication-documentation commit `325fe3b69fe6127808b1fdb0dcf3e3d707635a1a`
advanced main after the release tag. Its Node 22/24 CI run `31592931697` passed.

The configured global source checkout then advanced to that main commit. It
preserved five pre-existing untracked DAG research documents. Fresh
`npm ci --workspaces=false`, TypeScript, package verification, generated-dist
comparison, and packed observation probes passed. This installed rc.14 for new
Pi processes without stopping the separate active DAG work.
