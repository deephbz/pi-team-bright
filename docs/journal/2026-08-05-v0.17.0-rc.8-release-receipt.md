# Pi Team Bright v0.17.0-rc.8 release receipt

Date: 2026-08-05
Status: published

## Authorities

- Release source: `6540b828425ab1ae5c4d6836fd822f8952f2df48`.
- Annotated tag: `v0.17.0-rc.8`; its peeled target is the release source.
- npm package: `@hypercarrier/pi-team-bright@0.17.0-rc.8`.
- npm `next`: `0.17.0-rc.8`; `latest`: `0.17.0-rc.1`.
- GitHub prerelease:
  <https://github.com/deephbz/pi-team-bright/releases/tag/v0.17.0-rc.8>.

Rc.7 remains immutable and published. Rc.8 is the higher follow-up requested by
the owner.

## Contract and documentation change

`leader_share` now accepts values strictly greater than `0.1` and strictly less
than `1.0`; its default remains `0.6`. The README contains one combined Team and
Worker settings example and states Worker tool-list precedence.

Worker tool projection remains additive and subtractive. It starts with the
inherited Worker active list, adds known registered names from `enable`, then
removes known registered names from `disable`. Thus `[A, B, C]` with `enable:
[E]` and `disable: [C]` becomes `[A, B, E]`. Disable wins when both lists contain
the same name. Unknown names produce a diagnostic. Settings cannot restore
leader-only tools or grant authorization.

## Verification and publication

The two focused pane-layout and model-tool schema files passed 14 tests. Type
checking, generated contract review, the packed observation probe, and generic
generated-dist verification passed. The rc.7 broad evidence remained valid for
unchanged behavior, so no broad local matrix was repeated.

Exact-source CI
[30993756729](https://github.com/deephbz/pi-team-bright/actions/runs/30993756729)
passed on Node 22 and 24. Tagged OIDC publication run
[30994064679](https://github.com/deephbz/pi-team-bright/actions/runs/30994064679)
used `v0.17.0-rc.8` at the exact source. Its full tests, lane closure, package
verification, and npm publication passed. A separate hosted dry run was not
repeated for this narrow higher follow-up.

## Registry and provenance proof

The registry package has 84 files, 205,435 packed bytes, and 881,393 unpacked
bytes. Its immutable digests are:

- npm shasum: `402bba56d307399a685495ca87d253b10e86ed2f`;
- npm integrity:
  `sha512-Rbc1DUomZ9N11hyNv7womtlGN7sBEWQ86UQstCnl3b/KvV/xDbyMX5eZkc9QM21XPGNqKCjR4/gZ6fKziqkI2w==`;
- SHA-256:
  `bf082d064a07fc2e7a890176c132c962bd153e793a368f9e410230387745f4d6`;
- SHA-512:
  `45b7350d4a2667d375d61c8dbfbc289ad94637bb0111643ce9442cb429e5ddbfcabd5ff10dbc8c5f979991cf50336d573c636a2828d1e3f819e9f2b38aa908db`.

npm exposes publish and SLSA attestations. Both subjects contain the exact
package SHA-512. SLSA identifies repository `deephbz/pi-team-bright`, workflow
`.github/workflows/publish.yml`, ref `refs/tags/v0.17.0-rc.8`, the exact release
source, and run `30994064679`.

## Privacy and architecture

The staged and exact post-baseline range privacy scans passed. The required
full-history scan reported only grandfathered pre-baseline findings; its private
receipt remains outside the repository. Commit and tag identity use the approved
public handle, noreply email, and UTC dates. GitHub Actions OIDC published the
package.

Architecture impact: **none**. This release adjusts one adapter validation range
and documentation. It adds no component, authority, store, dependency direction,
data flow, trust boundary, or deployment topology.
