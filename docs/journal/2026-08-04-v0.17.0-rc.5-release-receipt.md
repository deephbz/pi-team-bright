# Pi Team Bright v0.17.0-rc.5 release receipt

Date: 2026-08-04
Status: published

## Authorities

- Release source commit: `78ced388926e2cc66007e01a1a2573d73e400b3c`.
- Annotated Git tag: `v0.17.0-rc.5`; its peeled target is the release source commit.
- npm package: `@hypercarrier/pi-team-bright@0.17.0-rc.5`.
- npm dist-tag: `next` points to `0.17.0-rc.5`; `latest` remains
  `0.17.0-rc.1`.
- GitHub prerelease:
  <https://github.com/deephbz/pi-team-bright/releases/tag/v0.17.0-rc.5>.

## Reversible gates

The clean detached source checkout packed 79 files once. The tarball is 219,296
bytes and unpacks to 957,029 bytes. Its digests are:

- npm shasum: `085290b89cba03660370595cc7fa76abfd626380`;
- npm integrity:
  `sha512-Op7fJUjbJxMfsvT8DiLogqTD0yNh426duFVRyujZajUWJDipzxnXFZ/BkgoNxMS4u5mC7WGmpVAlEqiCLV8l/Q==`;
- SHA-512:
  `3a9edf2548db27131fb2f4fc0e22e882a4c3d32361e36e9db85551cae8d96a35162438a9cf19d7159fc1920a0dc4c4b8bb9982ed61a6a5502512a8822d5f25fd`.

An isolated install loaded version `0.17.0-rc.5`. A bounded no-model Pi RPC
process ran from an unrelated directory with a disposable agent directory. It
discovered the package's `pi-team-bright` extension command.

The local aggregate found one test-environment fault after npm workspace cleanup
removed the package-local `ts-node` path used by a multiprocess lock fixture.
The focused lock file passed all five tests after restoring that dependency
resolution. No source changed for this environment repair. Exact-source CI then
passed on Node 22 and 24 in
[run 30925936413](https://github.com/deephbz/pi-team-bright/actions/runs/30925936413).

GitHub Actions dry run
[30926221269](https://github.com/deephbz/pi-team-bright/actions/runs/30926221269)
used exact head `78ced388926e2cc66007e01a1a2573d73e400b3c`. All gates passed,
and the npm publish step was skipped.

## Publication and registry proof

Tagged GitHub Actions run
[30926616110](https://github.com/deephbz/pi-team-bright/actions/runs/30926616110)
used `v0.17.0-rc.5` at the exact release source commit. The full lane passed 70
test files and 563 tests. Test-lane closure reported 52 fast and 18 exhaustive
files. Packed observation and generated-dist verification passed. The OIDC npm
publish step succeeded with dist-tag `next`.

The registry tarball is byte-identical to the recorded local tarball. Its size,
SHA-1, SHA-512, npm integrity, file count, and unpacked size match. npm exposes
publish and SLSA provenance attestations. Both subjects have the recorded
tarball SHA-512. SLSA provenance identifies repository
`https://github.com/deephbz/pi-team-bright`, ref
`refs/tags/v0.17.0-rc.5`, exact source commit
`78ced388926e2cc66007e01a1a2573d73e400b3c`, and run `30926616110`.

## Privacy and architecture

The final post-baseline range privacy scan passed. The full-history scan still
rejects grandfathered pre-baseline findings and printed no detected value. Its
private receipts remain outside the repository under `/tmp`. Commit and tag
identity use the approved public handle, noreply email, and UTC dates. No local
npm credential was used; publication used GitHub Actions OIDC.

Architecture impact: **none** for publication. The release carries accepted
public Task, trust, and pane-layout contract changes, but publication adds no
component, authority, persistence store, dependency direction, trust boundary,
or deployment topology.
