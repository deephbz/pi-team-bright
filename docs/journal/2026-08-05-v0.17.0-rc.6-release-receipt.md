# Pi Team Bright v0.17.0-rc.6 release receipt

Date: 2026-08-05
Status: locally verified and frozen; VCS and publication evidence pending

## Authorities

- Prepared source base: `27a532d1c9c9696afe3790c081028aae8af77d76`.
- The source base is the accepted rc.6 repair tree's HEAD. The tree remained
  uncommitted during preparation so the release source commit is pending.
- npm package: `@hypercarrier/pi-team-bright@0.17.0-rc.6`.
- Planned npm dist-tag: `next`; registry publication is pending.
- Planned Git tag: `v0.17.0-rc.6`; Git commit, push, tag, workflow dispatch, and
  GitHub prerelease evidence are pending.

## Reversible gates

The focused pack inspection used:

```text
npm pack --dry-run --json --ignore-scripts
```

The final prepared package manifest has 84 files, a packed size of 205,137
bytes, and an unpacked size of 880,632 bytes. npm reported shasum
`a7b7b0729e2f36911571311f774e82b6c0ea5d98` and integrity
`sha512-AZz+JRu1JZRx7DFGt09FG+czPSoLOIX8lzE3nXeaua606j4U0/7jqMCHJxsvWj+v3kyNhwcVXeROIbAMMvxHlw==`.

The manifest recipe is the canonical JSON encoding of package name, version,
filename, packed and unpacked size, shasum, integrity, file count, and the npm
file list with each file's path, size, and mode, in npm's reported order. Its
SHA-256 is:

`345af97c948b626f8e34ecd3631020d90f57d9c9ade1d5b5b965e51fdf20c9f0`

This is a dry-run receipt. It is not proof of a registry tarball or provenance
attestation.

A focused `git-privacy-scan --ref HEAD history` ran and returned nonzero. Its
output remains outside the repository and no detected value is recorded here.
The final release-range and full-history privacy gates remain pending for the
lead-only publication boundary.

## Prepared source

- Authoritative version markers are `0.17.0-rc.6` in package metadata, lockfile,
  model-tool implementation fence, README install/image references, and release
  parity documentation.
- The generated model-tool review HTML was regenerated from the current catalog
  and design source.
- The accepted uncommitted repair tree was preserved. No source reset,
  reconstruction, commit, tag, push, workflow dispatch, package publication, or
  release action ran during preparation.
- Architecture impact: **none**. This release changes no component identity,
  responsibility, authority, persistence, trust boundary, dependency direction,
  data flow, or deployment topology.

## Focused gate-repair effect

The saved rc.6 gate repair applied the smallest fixture and adapter-invariant fixes across the exact failing files. The focused checks passed: clean-cut E2E 3 tests, tool-result QA 1 test, narrow adapter coverage 3 tests, Worker create replay 1 test, and lock race 5 tests. The lock race fixture now resolves the workspace-installed `ts-node/register/transpile-only` without changing lock production code.

Architecture impact: **none**.

The generated declaration acceptance retained `dist/utils/models.d.ts` as the deterministic rc.6 delta. The focused verifier accepts its frozen diff hash and the two deterministic declaration support files emitted by the observation build.

## Final local release gates

- `TERM_PROGRAM=iTerm.app npm run test:full` passed 72 files and 579 tests.
- `npm run test:lanes` passed closure with 54 fast and 18 exhaustive files.
- `npm run verify:package` passed once after the deterministic declaration was
  accepted. The packed observation probe and generated-dist check passed.
- Package-verification manifests before and after were identical at
  `3048858dafa7f3512e8c6ed34590b64cf773beff482cf32d5538b45f6be99fb0`.
- The final package dry run produced the 84-file package evidence above.

No full suite or lane command was repeated after it passed. No commit, push,
tag, workflow dispatch, registry publication, or GitHub release action ran.

## Pending lead-only evidence

- [x] Independent broad package/release verification on the exact frozen tree.
- [ ] Final privacy and stale-reference gates at the release boundary.
- [ ] Normal VCS commit and push with the approved public identity.
- [ ] Git tag `v0.17.0-rc.6` and exact source-commit evidence.
- [ ] Tagged publish workflow through npm OIDC with dist-tag `next`.
- [ ] Registry tarball byte, size, integrity, file-count, and unpacked-size match.
- [ ] npm provenance, npm `next`, GitHub prerelease, and source-commit match.

No private paths, local account identifiers, credentials, or unpublished
registry values are part of this public receipt.
