# Pi Team Bright v0.17.0-rc.6 release receipt

Date: 2026-08-05
Status: aborted before npm publication

## Authorities

- Source commit: `4cfb0b49618c4a50484b3ac3d70e90bf0d5b2800`.
- Annotated Git tag: `v0.17.0-rc.6`; its peeled target is the source commit.
- npm package `@hypercarrier/pi-team-bright@0.17.0-rc.6` does not exist.
- npm `next` remained at `0.17.0-rc.5`.
- No GitHub Release was created for rc.6.

The public tag is immutable release evidence. The correction uses the higher
version `0.17.0-rc.7`; rc.6 will not be retagged or published.

## Reversible gate evidence

The local source gate passed 72 test files and 579 tests. Test-lane closure
reported 54 fast files and 18 exhaustive files. The packed observation probe
also passed after the generated declaration was refreshed.

Final review then found that `scripts/verify-generated-dist.cjs` had been
weakened with an rc.6-specific diff hash and an allowlist for ignored generated
files. That exception made a dirty release tree pass instead of requiring every
generated dependency to be tracked. The source tag was therefore not eligible
for publication.

The post-baseline privacy range scan passed. The full-history scan reported
only grandfathered pre-baseline findings; its private receipt remains outside
the repository.

## Aborted publication

GitHub Actions run
[30990508968](https://github.com/deephbz/pi-team-bright/actions/runs/30990508968)
was dispatched against the exact rc.6 tag, then canceled during `test:full`.
The lane-closure, package-verification, and npm-publish steps did not run. The
npm publish step is recorded as skipped, so no registry bytes or provenance
attestation exist for rc.6.

## Correction

Rc.7 restores the generic generated-dist invariant and tracks the emitted
`TaskVersionRef` declaration and JavaScript dependencies. Its separate release
receipt owns all later test, package, privacy, workflow, registry, provenance,
and GitHub Release evidence.

Architecture impact: **none**. Aborting rc.6 and correcting release-source
verification changes no component responsibility, authority, dependency,
data flow, persistence or trust boundary, or deployment topology.
