# Pi Team Bright v0.17.0 release receipt

Date: 2026-08-14
Stage: sharing

Stable release source is
`82c31a91d0f1c2f6cb69ae86dc8df5272868cb58`, tree
`44253927810f21c06deeecfaa9af5e7817510dbd`. Annotated tag `v0.17.0` has tag
object `4c0ec2766abf5ee3121be5ead4d8058bed56f75c` and targets that exact source.

## Product acceptance

A fresh independent Pi process loaded the exact stable source through isolated
settings. Team `ptb-v0170-release-e2e-20260814b` used two stable Terra-medium
Workers and exercised all nine leader tools plus all three Worker tools.

Six Tasks reached `goal_achieved`. The bounded repair route was
`criterion@1 -> build@2 -> criterion@2`. The join bound accepted Attempts
`criterion@2` and `notes@1`. Graph revision replay returned `replayed: true`,
and changed reuse of the same operation identity refused without state change.
Both Workers were reused, then stopped. Team shutdown retained no unfinished
Task IDs.

The E2E operator initially withheld Worker Alert acceptance because its visible
`team_sync` result omitted that event. Independent durable evidence resolved the
gap. Team event cursor 14 records clarification `V017_WORKER_ALERT_PASS`; the
exact verifier Session records one successful `alert_send`, five successful
`task_read` results, and Worker-authored `task_update` calls. Local machine
receipts remain outside Git under `/tmp/ptb-v0170-release-e2e-*`.

## Source, package, and privacy gates

Executable source `f06c450263d2b43b9ccb41b36107da21821feb6d` passed the
complete local release lane before the final documentation-only evidence
amendment. The aggregate passed 142 files and 1,007 tests; 8 files and 16 causal
variants were intentionally skipped. Lane closure reported 143 files: 120 fast
and 23 exhaustive. Package, generated-output, graph-sentinel, JSON, link, and
diff gates passed.

The exact tagged source then passed main CI run `31812399127` on Node 22 and
Node 24. Hosted dry-run `31813016892` passed its full aggregate, lane, and
package gates and skipped publication.

The exact final full-history privacy scan still rejects 100 inherited findings
across 24 commits. Every finding commit is an ancestor of publication baseline
`325fe3b69fe6127808b1fdb0dcf3e3d707635a1a`; this does not certify inherited
history. The exact candidate range after that baseline passed, as did the
normal pre-push privacy hook.

## Exact one-pack artifact

A clean detached checkout of the tagged source packed once. The exact tarball
installed in an isolated project and passed:

- CommonJS and TypeScript observation exports;
- producer version `0.17.0`;
- owned Beads `1.1.0` launcher;
- bounded no-model Pi RPC extension loading for `pi-team-bright` and
  `pi-team-graph`.

Artifact identity:

- Files: `197`
- Packed bytes: `330302`
- Unpacked bytes: `1430607`
- npm shasum: `e3e3c0af21adee8ec7b337e18be0f10980b2422c`
- npm integrity:
  `sha512-DsgObCO73Cixd3J7fKmJzBa6DrM/CPVpaiifSeoZQTyG0zzVfBxSPiFNEZhD3OBm9sl4B7MCUAlPu/sAROCROQ==`
- SHA-256: `c8e05a4f0cddcb652c54abf006e98d23bd44a3418a0b9314576190add1f42b4e`
- SHA-512:
  `0ec80e6c23bbdc28b177727b7ca989cc16ba0eb33f08f5696a289f49ea19413c86d33cd57c1c523e214d119843dce066f6c97807b30250094fbbfb0044e09139`
- Path-sorted manifest SHA-256:
  `9a2951d58ad94b709b31baca0cdb4e0457ce5da829c50e6f0d1914f107406ae4`

## Hosted publication and registry proof

Hosted OIDC publish run `31813616378` ran from `refs/tags/v0.17.0`. It passed
full tests, lane closure, and package verification before running
`npm publish --access public --provenance --tag latest`.

npm now reports:

- `@hypercarrier/pi-team-bright@0.17.0`;
- `latest` -> `0.17.0`;
- `next` -> `0.17.0-rc.14`.

The downloaded registry tarball is byte-identical to the one-pack artifact. Its
size, file count, shasum, integrity, SHA-256, and SHA-512 match this receipt.
Registry metadata exposes both npm publish and SLSA provenance attestations.
Both subjects match the recorded tarball SHA-512. SLSA build parameters name
repository `https://github.com/deephbz/pi-team-bright`, workflow
`.github/workflows/publish.yml`, and ref `refs/tags/v0.17.0`; the annotated tag
binds that ref to the exact release source.

Stable GitHub Release:
https://github.com/deephbz/pi-team-bright/releases/tag/v0.17.0

GitHub Releases remain release-history authority. The reviewed public notes are
[`../release/v0.17.0-release-notes.md`](../release/v0.17.0-release-notes.md).
Downstream HyperCarrier gitlink updates are separate composition evidence.
