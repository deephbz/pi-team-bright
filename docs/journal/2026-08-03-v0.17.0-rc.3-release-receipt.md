# Pi Team Bright v0.17.0-rc.3 release receipt

Date: 2026-08-03
Status: published

## Authorities

- Release source commit: `315c572ee023c0d704e7b813da92bcc077614ed4`.
- Annotated Git tag: `v0.17.0-rc.3`; its peeled target is the release source
  commit.
- npm package: `@hypercarrier/pi-team-bright@0.17.0-rc.3`.
- npm dist-tag: `next` points to `0.17.0-rc.3`; `latest` remains
  `0.17.0-rc.1`.
- GitHub prerelease:
  <https://github.com/deephbz/pi-team-bright/releases/tag/v0.17.0-rc.3>.

## Reversible gates

The clean detached source checkout packed 75 files once. The tarball is 210,091
bytes with these digests:

- npm shasum: `cbd6a662bf7145f73ffac3e3416353608e687bcb`;
- npm integrity:
  `sha512-FWtRUha3yOkd8XMbGBCBps/JwlcMZXBWR7KeVq6rKRvvNL3BlbNhzlxrNmcr4tP1Ck7B3JNiQxaA/WrcjrepCw==`;
- SHA-512:
  `156b515216b7c8e91df1731b181081a6cfc9c2570c65705647b29e56aeab291bef34bdc195b361ce5c6b36672be2d3f50a4ec1dc9362431680fd6adc8eb7a90b`.

An isolated install loaded version `0.17.0-rc.3`. A no-model Pi RPC process ran
from an unrelated directory with a disposable agent directory and returned the
package's `pi-team-bright` command.

GitHub Actions dry run
[30822624772](https://github.com/deephbz/pi-team-bright/actions/runs/30822624772)
used exact head `315c572...`. All gates passed, and the npm publish step was
skipped.

## Publication and registry proof

Tagged GitHub Actions run
[30822920344](https://github.com/deephbz/pi-team-bright/actions/runs/30822920344)
used `v0.17.0-rc.3` at exact head `315c572...`. The full lane passed 69 files
and 545 tests. Test-lane closure reported 51 fast and 18 exhaustive files.
Packed observation and generated-dist verification passed. The OIDC npm publish
step succeeded with dist-tag `next`.

The registry tarball is byte-identical to the recorded local tarball. Its size,
SHA-1, SHA-512, npm integrity, file count, and unpacked size match. npm exposes
both publish and SLSA provenance attestations. Their subject SHA-512 matches the
recorded tarball. SLSA provenance identifies repository
`https://github.com/deephbz/pi-team-bright`, workflow
`.github/workflows/publish.yml`, ref `refs/tags/v0.17.0-rc.3`, and run
`30822920344`.

## Stress investigation and privacy

The `rc-stress-16` generic error cluster came from implementation
`0.17.0-rc.2`. Persisted evidence contained 92 Pi Team tool errors: 28 rejected
compound claims and 64 Beads timeouts. It contained no Pi Team semantic-result
or model-projection validator failure. rc.3 preserves raw error `content` and
`details`, so the old generic renderer artifact did not block this release. The
old run is not an rc.3 16-Worker capacity claim
(`docs/journal/2026-08-03-rc-stress-16-investigation.md`).

The staged and final post-baseline range privacy scans passed. The full-history
scan at the tagged commit still rejects already-public, grandfathered
pre-baseline history; it printed no detected value in this release process. Its
private receipt remains outside the repository at
`/tmp/pi-team-bright-rc3-tag-history-privacy.txt`. No local npm credential was
used; publication used GitHub Actions OIDC.

Architecture impact: **none** for publication. The release carries the Task and
TUI contract changes recorded in
`docs/journal/2026-08-03-rc3-last-mile-adjustments.md`; it changes no component,
authority, dependency, data flow, persistence, trust, or deployment boundary.
