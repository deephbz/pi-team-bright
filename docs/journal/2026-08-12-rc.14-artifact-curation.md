# rc.14 artifact curation record

Date: 2026-08-12
Stage: hardening
Scope: documentation curation only

## Worker preflight

This Worker recorded provider `openai-codex`, model `gpt-5.6-terra`, and
medium reasoning before artifact review. Proxy-name presence was recorded for
`HTTP_PROXY`, `HTTPS_PROXY`, `ALL_PROXY`, and `NO_PROXY`. Proxy values were not
recorded.

The required Worker model is `openai-codex/gpt-5.6-terra` at medium reasoning,
so this documentation task continued. This record applies only to this Worker.
It does not replace historical leader or Worker evidence.

## Curated current status

The shared tree has uncommitted liveness and Worker-recovery changes. The
payload-free incident artifact records owner-supplied macOS diagnostic facts.
It does not prove repair correctness.

No receipt proves a new-source real macOS Pi/Herdr eight-Worker stress, safe
Worker recovery E2E, no FSEvents feedback storm, no overlapping sync-read
Beads children, bounded `team_sync`, or exact Worker stops. The reserved
aggregate has not run for this tree. Therefore rc.14 has no selected source
candidate, commit, tag, push, package, registry, provenance, GitHub release,
or publication claim.

The current release remains `0.17.0-rc.13`, published to npm `next` from
annotated tag `v0.17.0-rc.13`. The incident continuation, release plan, release
notes draft, and checklist remain preparation artifacts. They do not establish
an rc.14 release.

## Updated projections

- The semantic-hardening context and result now state the current incident
  blocker and point to this record.
- The incident continuation records this Worker preflight without changing its
  historical Sol-leader evidence.
- The rc.14 release plan records this preflight as non-release evidence.

No source, test, package, release, or Team-lifecycle action occurred during
this curation task.
