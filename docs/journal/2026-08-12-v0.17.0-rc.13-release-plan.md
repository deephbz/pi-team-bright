# v0.17.0-rc.13 release plan

Date: 2026-08-12
Stage: sharing
Source preparation base: `d2dfc7004d73f7a69c2a8cf23bd4c691162fe51a`
Architecture impact: none from this release-metadata preparation

## Purpose

Prepare the rc.13 source artifacts from the exact stated base. The candidate
updates package identity, README install and image references, release parity,
maintained current status, and semantic-hardening result status. It does not
change executable behavior or public contracts.

## Source preparation

Set package and lock metadata to `0.17.0-rc.13`. Point the package image and
README install command to rc.13. Record the same candidate state in the parity
checklist, maintained context, and semantic-hardening result.

This plan records source preparation only. It creates no rc.12 work, tag, push,
npm publication, npm `next` change, GitHub release, registry check, provenance
claim, or final verification claim.

## Required order

1. Commit the exact rc.13 source candidate.
2. Run the reserved aggregate lane on that committed source.
3. Run package and release checks and record exact evidence.
4. Under the owner authorization for this release, push only after all reversible gates pass, then complete the hosted dry run, tag, OIDC publication, registry/provenance proof, GitHub prerelease, and publication receipt in the documented order.

Do not run the release aggregate before the source-candidate commit. That check
must identify the committed source it verifies.

## Source-preparation checks

Before the source-candidate commit, check package and lock version consistency,
JSON parsing, changed Markdown links, and the Git diff. These checks establish
only source-artifact readiness. They do not prove aggregate verification,
package behavior, registry state, provenance, or publication readiness.

## Deferred release operations

Tagging, pushing, npm publishing, npm `next` changes, registry and provenance
verification, GitHub prerelease creation, and a final release receipt remain
separate operations after the exact committed candidate passes its reversible
release gates. The owner has authorized these operations for rc.13.
