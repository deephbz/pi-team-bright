# Pi Team Bright v0.17.0-rc.7 release receipt

Date: 2026-08-05
Status: correction prepared; VCS and publication evidence pending

## Authorities

- Correction base: `4cfb0b49618c4a50484b3ac3d70e90bf0d5b2800`.
- Planned npm package: `@hypercarrier/pi-team-bright@0.17.0-rc.7`.
- Planned npm dist-tag: `next`.
- Planned annotated Git tag: `v0.17.0-rc.7`.
- The rc.6 tag remains aborted and unpublished; its receipt owns that evidence.

## Source correction

Rc.7 restores the generic generated-dist verifier. It removes the
release-specific accepted-diff hash and ignored-file allowlist from rc.6. The
observation build's `TaskVersionRef` declaration and JavaScript dependencies
are tracked as release files, so a clean checkout can reproduce and verify the
complete generated package.

The canonical Task and Beads-call changes are otherwise unchanged from the
locally verified rc.6 source. Architecture impact is **none**.

## Local gates

The functional source gate passed 72 test files and 579 tests. Test-lane closure
reported 54 fast and 18 exhaustive files before this release-source-only
correction. Rc.7 typecheck, the generic generated-dist check, and
`npm run verify:package` passed. The packed observation probe loaded the public
CommonJS and TypeScript surfaces.

The final package dry run contains 84 files, 205,148 packed bytes, and 880,747
unpacked bytes. npm reported shasum
`0e0a9c90400e1374b15646314c9543b0fea722ad` and integrity
`sha512-R3ke/M+ADas5hSwDEOi/DaDO7/RX6RcVnj6OUE/Z0EIvWZUb7zgtw3/FjYFFJeaBHhXsBPblwEVkEst0DeED+w==`.
The canonical dry-run manifest SHA-256 is
`557246f555f417f5f799a90dceccff26456ff0389f249c52aca2585c73ed706b`.
This dry run is package-content evidence, not a registry or provenance claim.

- [x] Generic generated-dist verification.
- [x] Packed observation probe and package verification.
- [x] Final package dry-run manifest and digests.
- [x] Staged privacy gate; post-baseline range and full-history gates remain pending.

## Publication evidence

- [ ] Exact source commit and clean detached package.
- [ ] Isolated install and no-model Pi package canary.
- [ ] GitHub Actions dry run on the exact source commit.
- [ ] Annotated tag `v0.17.0-rc.7` at that commit.
- [ ] Tagged GitHub Actions npm OIDC publication under `next`.
- [ ] Registry byte, shasum, integrity, size, and file-count match.
- [ ] npm provenance, npm `next`, and GitHub prerelease match the source.

No private path, credential, local account identifier, or unpublished registry
value belongs in this receipt.
