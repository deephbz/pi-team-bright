# Pi Team Bright v0.17.0-rc.7 release receipt

Date: 2026-08-05
Status: published

## Authorities

- Release source commit: `5bff63986f51581ae96555aa18b91c1f90723e9f`.
- Annotated Git tag: `v0.17.0-rc.7`; its peeled target is the release source.
- npm package: `@hypercarrier/pi-team-bright@0.17.0-rc.7`.
- npm dist-tag: `next` points to `0.17.0-rc.7`; `latest` remains
  `0.17.0-rc.1`.
- GitHub prerelease:
  <https://github.com/deephbz/pi-team-bright/releases/tag/v0.17.0-rc.7>.

The `v0.17.0-rc.6` source tag was aborted before npm publication. Its receipt
preserves the canceled workflow evidence. Rc.7 is the higher correction; rc.6
was not retagged or published.

## Source correction

Rc.7 restores the generic generated-dist verifier. It removes the
release-specific accepted-diff hash and ignored-file allowlist from rc.6. The
observation build's `TaskVersionRef` declaration and JavaScript dependencies
are tracked release files, so a clean checkout reproduces the complete package.
The canonical Task and Beads-call changes are otherwise unchanged from the
verified rc.6 source.

## Local and exact-source gates

The functional source gate passed 72 test files and 579 tests. Test-lane closure
reported 54 fast and 18 exhaustive files. Rc.7 typecheck, generic generated-dist
verification, packed observation probe, and package verification passed.

Exact-source CI passed on Node 22 and 24 in
[run 30991108929](https://github.com/deephbz/pi-team-bright/actions/runs/30991108929).
GitHub Actions dry run
[30991493190](https://github.com/deephbz/pi-team-bright/actions/runs/30991493190)
used the exact release commit. The full lane, test-lane closure, and package
verification passed; npm publication was skipped.

A clean detached checkout packed the source once. The exact tarball installed
in an isolated project, loaded version `0.17.0-rc.7`, and exposed the public
observation module. A bounded no-model Pi RPC process ran from the isolated
project with a disposable agent directory and discovered the package's
`pi-team-bright` extension command.

The tarball contains 84 files, 205,148 packed bytes, and 880,747 unpacked bytes.
Its digests are:

- npm shasum: `0e0a9c90400e1374b15646314c9543b0fea722ad`;
- npm integrity:
  `sha512-R3ke/M+ADas5hSwDEOi/DaDO7/RX6RcVnj6OUE/Z0EIvWZUb7zgtw3/FjYFFJeaBHhXsBPblwEVkEst0DeED+w==`;
- SHA-512:
  `47791efccf800dab39852c0310e8bf0da0ceeff457e917159e3e8e504fd9d0422f59951bef382dc37fc58d814525e6811e15ec04f6e5c0456412cb740de103fb`;
- package-manifest SHA-256:
  `557246f555f417f5f799a90dceccff26456ff0389f249c52aca2585c73ed706b`.

## Publication and registry proof

Tagged GitHub Actions run
[30991800392](https://github.com/deephbz/pi-team-bright/actions/runs/30991800392)
used `v0.17.0-rc.7` at the exact release commit. Full tests, lane closure,
package verification, and the GitHub OIDC npm publish step passed with dist-tag
`next`.

The registry tarball is byte-identical to the clean detached tarball. Its size,
SHA-1, SHA-512, npm integrity, file count, and unpacked size match. npm exposes
both publish and SLSA provenance attestations. Each subject has the recorded
tarball SHA-512. The SLSA statement identifies repository
`https://github.com/deephbz/pi-team-bright`, workflow
`.github/workflows/publish.yml`, ref `refs/tags/v0.17.0-rc.7`, exact release
commit `5bff63986f51581ae96555aa18b91c1f90723e9f`, and run `30991800392`.

## Privacy and architecture

The staged and post-baseline range privacy scans passed. The required
full-history scan reported only grandfathered pre-baseline findings; its receipt
remains outside the repository and no detected value is recorded here. Commit
and tag identity use the approved public handle, noreply email, and UTC dates.
No local npm credential was used; publication used GitHub Actions OIDC.

Architecture impact: **none**. The release changes Task and adapter contracts,
but adds no component, authority, persistence store, dependency direction,
data flow, trust boundary, or deployment topology.
