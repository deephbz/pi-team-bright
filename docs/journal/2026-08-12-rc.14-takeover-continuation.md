# rc.14 takeover continuation

Date: 2026-08-12
Stage: hardening before sharing
Team: `rc14-sol-takeover` (active)
Base: `origin/main` and initial HEAD `53367e412ad0217bfcf4845d92a07bb9ebec6de2`
Release state: no candidate commit, aggregate, tag, push, publication, provenance, or GitHub rc.14 release

## Leader and Worker contract

The exact leader is `openai-codex/gpt-5.6-sol` at high reasoning. Do not change it to Terra. Worker default is `openai-codex/gpt-5.6-terra:medium`. The leader and all four Workers recorded the required proxy-variable presence. The replacement `contract-review` carrier also recorded Terra-medium and proxy-name presence before it resumed inspection.

The owner authorized takeover after locked rescue of old Memberships. External rescue receipt: `/tmp/rc14-direct-leader-rescue-20260812T090610Z.json`. The new active Team epoch is `rc14-sol-takeover`.

## Current source work

The shared uncommitted tree preserves prior Worker changes. Do not revert it. Intended visible changes remain only:

1. bounded, responsive `team_sync` under runtime and macOS FSEvents noise;
2. valid fast Worker recovery is not killed by the leader.

No Beads change is authorized. Preserve public tools, schemas, exports, persisted shapes, filenames, defaults, observation semantics, and authority meaning.

Recovery work now:

- uses a pure named runtime recovery preflight instead of sentinel PID `-1`;
- passes exact Membership identity to the child;
- lets the child alone claim runtime and bind its Pi Session;
- serializes and revalidates recovery under the exact Membership lease;
- reuses a concurrently recovered live carrier, so concurrent ensure spawns at most one candidate;
- persists the exact candidate terminal target;
- stops only the exact candidate if target persistence fails;
- cleans only the stale unused aggregate on concurrent reuse;
- retains fail-closed bound-Membership missing/malformed/mismatched runtime rules and prepared first-binding parity.

Focused recovery evidence passed 43 tests. The exact full-config launch-compensation plus ergonomic command passed 31 tests. Typecheck and diff checks passed. Earlier independent P1 findings were corrected; their Task evidence remains historical.

Liveness work now:

- reads runtime snapshots without the writer lock while writers use atomic replacement;
- filters watcher hints to exact string runtime `*.json` names and exact `team-events.jsonl`;
- ignores null, undefined, empty, Buffer, temporary, lock, cursor, and unrelated filenames;
- keeps one running waiter scan plus one coalesced pending scan with authority priority;
- retains the five-second authority cadence and final authoritative recheck;
- retains bounded timeout and cancellation under noise;
- keeps one private in-flight complete Task-projection Promise per `CoordinationObservationService` and Team, so orphaned waiter scans and final or later reads join it;
- clears the single-flight entry only after settlement and by Promise identity, then a later read starts fresh.

The owner directly added the per-Team single-flight in `src/coordination/observation-service.ts` and its initial equivalence test. A four-file focused lane passed 52 tests; typecheck and diff checks passed. The liveness Worker was assigned to add or confirm deterministic timeout and cancellation lifetime tests before closing.

A non-final `onUpdate` progress notice was assessed and rejected for this slice. It requires a callback coordinate across multiple ports and implementations, so it is not a simple schema-neutral repair.

## Runtime diagnosis and release blocker

Maintained payload-free receipt: `docs/journal/artifacts/2026-08-12-team-sync-macos-feedback-storm.json`.

Raw owner-supplied evidence remains outside the repository:

- `/tmp/ptb-team-sync-sample-20260812T091450Z.txt`, SHA-256 `fc8564e81c571b9d5c1e09b81a86502abf60e590ccb80afbab7c4f70bce97ae2`;
- `/tmp/ptb-team-sync-watchdog-sample-1786526275689.txt`, SHA-256 `f207c8e3dce701ec0297e8c292aaf17185cd1618addfd0adf02ac486d94e6eb5`;
- `/tmp/ptb-team-sync-watchdog-sample-1786526696276.txt`, SHA-256 `a33a780ca8fbc0b821502c49022b3a1e9f01e5605104f1dc1aa8685eb8dbb75d`;
- `/tmp/ptb-team-sync-watchdog-20260812.jsonl`, SHA-256 `c2bc285240045a3d87ddc5c030e98d2afd0c2fd49fa48af7fa214269fbb58cd1`.

Observed diagnostic facts include an 88,384 ms `team_sync`, a later 137,247 ms call, 4119/4122 and 2305/2306 main-thread FSEvents samples, and two concurrent live Beads list children started 29 seconds apart plus a zombie. The samples are diagnosis, not correctness proof. Do not kill children still owned by a live leader call.

Release is blocked until a new-source real macOS Pi/Herdr eight-Worker stress and safe recovery E2E stays bounded, shows no overlapping Beads children from sync reads, shows no FSEvents feedback storm, records `team_sync` durations/outcomes, proves the former stuck path, and records exact stop receipts. Do not claim pixels unless observed.

## Active Task continuation

Closed implementation/review evidence includes preflights, initial contract review, bounded liveness, recovery, null-filename correction, recovery expectation correction, and null-hint independent verification.

Current or pending authoritative Tasks:

- `rc14-sol-takeover-0nw`: liveness Worker owns complete Task-projection single-flight and timeout/cancellation lifetime proof. It was still active at this handoff.
- `rc14-sol-takeover-yac`: independent `contract-review` verification depends on `0nw`.
- `rc14-sol-takeover-c88`: final exact implementation diff review depends on `yac`.

Use `team_sync({view:"updates"})` for normal continuation. Do not poll panes or runtime as a work loop.

## Required next order

1. Let `0nw` close with timeout and cancellation single-flight proof.
2. Let `yac` independently verify per-service/Team scope, one underlying read, Promise-identity clearing, fresh post-settlement reads, and no semantic/public/persistence change.
3. Let `c88` run the combined focused liveness, recovery, Session, ergonomic, and compensation lane plus typecheck. Fix any blocker in its owning subsystem and repeat only focused checks.
4. Start a separate new-source real Pi/Herdr eight-Worker stress E2E. Use Terra-medium Workers with Worker-authored preflight events. Record durations, process-overlap observations, safe exact recovery, and stop receipts. Treat traces as diagnosis.
5. Update evergreen context, semantic-hardening result/context/journal, rc.14 plan, draft notes, checklist, and dated E2E receipt.
6. When every implementation Task is terminal and the exact tree is stable, prepare and commit the rc.14 candidate. Only then run the reserved aggregate exactly once.
7. Complete package/generated, lanes, agent/tool-result QA, public/persistence diff, graph, privacy, JSON/link, detached one-pack install, owned Beads, and Pi-load checks.
8. Publish only if `@hypercarrier/pi-team-bright@0.17.0-rc.14` remains absent. Use `deephbz` and the GitHub noreply identity, privacy hooks, normal Git commands, and OIDC workflow publication. Use `npm-auth` only for authenticated npm operations; do not inspect npm secret configuration.
9. Follow rc.13 order: clean candidate commit, push main, exact-source CI, hosted dry run, annotated tag, OIDC publish to npm `next`, SLSA provenance, GitHub prerelease, publication receipt commit and push, then local HEAD equals `origin/main` and the tree is clean.
10. Stop Workers only after all nonterminal Tasks resolve. Reconcile, then cleanly shut down the Team.

Do not touch rc.12 or DAG-native work. Do not run the reserved aggregate or release before the real new-source macOS E2E passes.
