# Pi Team Bright v0.17.0-rc.11 source verification receipt

Date: 2026-08-09
Stage: sharing
Verified source: `638d5934bd52c7f4a3fe18525e5d72569a227211`
Package metadata: `@hypercarrier/pi-team-bright@0.17.0-rc.11`
Publication state: untagged and unpublished
Architecture impact: changed internally for Task and Team dependency ownership;
public behavior and HyperCarrier's opaque internal view are unchanged

## Scope and source identity

This receipt records verification of exact clean source commit
`638d5934bd52c7f4a3fe18525e5d72569a227211`. The commit prepares rc.11 metadata
atop semantic-hardening implementation `15e707b`. The verification wrote no
source, test, package, or diagram file and created no commit.

This document is commit-relative evidence added after source verification. It is
not part of the verified source identity. At verification completion, no rc.11
tag, push, npm publication, npm `next` change, registry or provenance claim,
GitHub release, or final published-release receipt existed.

## One-time aggregate and source gates

The reserved full aggregate ran once under installed Node `22.22.1`:

```sh
fnm exec --using 22.22.1 -- env TERM_PROGRAM=iTerm.app npm run test:full
```

It passed 88 files and 695 tests in 253.59 seconds. It was not rerun. Test-lane closure passed with 88 files: 67 fast and 21 exhaustive.

Follow-up source gates passed:

- package verification and generated-distribution comparison;
- agent-surface and tool-result QA;
- public-surface, persistence, and package-metadata comparisons;
- all 48 tracked JSON files parsed;
- all 95 local Markdown link occurrences resolved;
- exact source, path, and diff checks remained clean.

The exact release range from the configured privacy baseline through
`638d5934` passed. The full-history scan still reports only known grandfathered
pre-baseline commit-metadata locations, and its output is byte-identical to the
configured-baseline scan. This limits the claim: the release range passed, but
this receipt does not certify grandfathered history for publication.

## Exact one-pack procedure

A clean detached checkout at `638d5934` used Node 22 and ran:

```sh
npm ci --no-audit --no-fund
npm pack --json --pack-destination "$PACK_DIR"
```

The `npm pack` command ran exactly once. A separate temporary project then ran:

```sh
npm init -y
npm install --no-audit --no-fund "$TARBALL"
```

The exact tarball contained 93 files. npm reported 225,058 packed bytes and
966,670 unpacked bytes.

- npm shasum: `60c1fb26cdc1ee79ea5fb5c880b25512af29794e`
- npm integrity:
  `sha512-mRnECg1tEIC7snHlT3SAO9ADeczDO6PVFV/wXuWPD66nUqrHX3vUVELwRrPJ8qK4wjdtue7F2/EqOFz2gCxC/w==`
- tarball SHA-256:
  `5e30158d7d573520478a06e28af911f89c6415e43a7a9d3222a009de58e02dd7`
- tarball SHA-512:
  `9919c40a0d6d1080bbb271e54f74803bd00379ccc33ba3d5155ff05ee58f0faea752aac75f7bd45442f046b3c9f2a2b8c2376db9eec5dbf12a385cf6802c42ff`
- path-sorted manifest SHA-256:
  `fa3f12b65c9cd7cd822de009f0ddce9c718e10a2ba07393bfc2c4d243344ffa9`
  The digest bytes are the path-sorted JSON `{path,size,mode}` entries plus one
  final LF.

## Installed-artifact canaries and limits

The installed exact tarball exposed the observation package through CommonJS
and TypeScript. Its owned Beads 1.1.0 binary ran from the installed package. A
bounded no-model Pi 0.84.1 RPC canary loaded the main `pi-team-bright` extension.

These checks prove installed package wiring, the observation exports, the owned
Beads binary, and main-extension load. They do not prove a full live Team
journey, terminal rendering, model behavior, or model interpretation. Existing
outside-in and focused evidence remains in the semantic-hardening
[result report](../projects/semantic-hardening/result.md), [subsystem
audit](../projects/semantic-hardening/subsystem-boundary-audit.md), and executable
tests.

## Verification boundary

Exact source `638d5934` passed its one reserved aggregate and the listed local
release gates. It remains an untagged, unpublished source candidate. Publication
operations, registry comparison, npm provenance, GitHub release evidence, and a
published-release receipt require separate authorization and external signals.
